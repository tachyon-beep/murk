# API Reference

The full Rust API documentation is generated by `rustdoc` and published
alongside this book.

**[Browse the API Reference (rustdoc) &rarr;](https://tachyon-beep.github.io/murk/api/murk_core/index.html)**

You can also build the API docs locally:

```bash
cargo doc --workspace --no-deps --open
```
